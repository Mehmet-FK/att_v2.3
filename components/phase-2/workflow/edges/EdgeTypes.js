import FloatingEdge from "@/components/phase-2/workflow/edges/FloatingEdge";
import { SmoothStepEdge } from "reactflow";

const edgeTypes = {
  floating: FloatingEdge,
  smoothstep: SmoothStepEdge,
};
export default edgeTypes;
